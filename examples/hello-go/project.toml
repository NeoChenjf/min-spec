# project.toml —— min-spec 真相源。门禁只读这里，不认语言。
# 换语言/换命令只改这一个文件。每条命令只看退出码（0=过，非零=挂）。

[project]
name = "hello-go"
language = "go.mod"  # 自动探测，纯标注，门禁不依赖

[gates]
# 缺省的门禁会被跳过（不报错）。下面是自动探测结果，请确认后删掉本行注释。
build = "go build ./..."  # 自动探测，请确认
test = "go test ./..."  # 自动探测，请确认
