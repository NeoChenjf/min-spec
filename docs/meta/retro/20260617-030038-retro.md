# Retro — 20260617-030038

> 交付内容：给 min-spec 补上「反哺反思」闭环——新增 retrospect（复盘沉淀）第 5 阶段。
> 本文件同时是该机制落地后的**第一个活样本**：用新加的 retro skill 复盘"加 retro 这件事"本身。

## 一句话概述
把流程从「门禁过即结束」的四阶段，扩成「门禁过后回头沉淀经验反哺下次」的五阶段闭环；
纯 Markdown + 现有脚本实现，零新依赖，`validate.py` / `gate.py` 双门禁通过。

## 做得好的
- **改前先认坑**：动手前确认了 `validate.py::check_dag` 强制「DAG 恰 1 个终点」，
  所以加阶段时同步把 `verify.next` 改成指向 retrospect，一次过校验，没踩两终点的雷。
- **三处同步一次到位**：workflow-dag / state-schema / validate 的 REQUIRED_FILES 同改，
  没漏（CLAUDE.md 早有铁律「改 DAG 三处同步」，照做即顺）。
- **守住轻量边界**：没把来源那套 feedback 事件模型 / 知识分层照搬进来（水土不服），
  只取「回读留痕→沉淀经验库」的内核，符合 min-spec「极简、语言无关、零依赖」的魂。

## 踩的坑
- 现象：发现 `harness/spec.md` 和执行清单里的阶段还停留在旧的 `plan→build→verify→report`，
  连 clarify 都没有、还有个早已不存在的 `report` 阶段——是本次之前就遗留的文档漂移。
  代价：差点只改 DAG 不改宪法文件，会让「只读宪法」继续说假话；额外花了一轮排查同步。
  下次怎么避：**plan 阶段就该把「同一概念散落在哪些文件」全列进影响面**——
  阶段定义至少散在 workflow-dag / state-schema / orchestrator / spec / AGENT / README / CLAUDE 七处，
  改阶段前先 `grep` 一遍旧表述，一次性圈定，别等改到一半才发现漏网。

## 沉淀到经验库的条目
- L-20260617-030038-1（适用 plan）：改「贯穿性概念」（如阶段名）前先全仓 grep 旧表述圈定影响面。
- L-20260617-030038-2（适用 build）：给 DAG 加阶段时，新增阶段成为新终点，必须同步把原终点的 next 指过去，否则 validate 的「恰 1 终点」校验挂。
