# Plan Note — {run_id}

> 使用说明：把本文件复制到 `docs/meta/plan/{run_id}-plan-note.md`，
> 填写各章节，删掉所有 `>` 提示行，完成后请用户确认再进入 build 阶段。

---

## 一、需求理解

**需求来源**：（issue 链接 / 需求描述 / 对话截图）

**我的理解**：
> 用自己的话复述需求，确保和用户理解一致。如果需求有歧义，在这里列出你的假设。

**假设与边界**：
- 假设 1：
- 假设 2：
- 不在本次范围内：

---

## 二、技术方案

> 描述你打算怎么实现。重点说清楚"改什么"和"为什么这样改"，不要直接贴代码。

**方案概述**：

**主要改动点**：
| 文件/模块 | 改动内容 | 改动原因 |
|---|---|---|
| `path/to/file.ts` | 新增函数 `foo()` | 实现 X 功能 |

**关键决策**：
> 如果有多个实现思路，说明为什么选这个，放弃了什么。

---

## 三、影响面分析

> 改动会波及哪些地方，逐一列出。

**直接改动的文件**：
- `path/to/file.ts`

**间接影响（调用方/依赖方）**：
- 无 / `path/to/caller.ts`（需要同步修改）

**接口/数据结构变化**：
- 无 / 新增字段 `xxx`，向后兼容 / 破坏性变更（说明迁移方式）

---

## 四、测试策略

**如何验证功能正确**：
> 说明你打算怎么测，gate 命令（`project.toml` 里的 `test`）能覆盖哪些场景。

**gate 命令**：`（project.toml 里的 test 命令）`

**手动验证步骤**（如果自动化不够）：
1. 步骤一
2. 步骤二

**不打算测的内容及原因**：

---

## 五、风险与遗留问题

| 风险/问题 | 严重程度 | 处理方式 |
|---|---|---|
| 例：依赖的第三方库版本不确定 | 低 | 本次先锁定版本，下次再升级 |

**本次不处理的技术债**：
- 无

---

## Reviewer 确认

> 实习生/Agent 填完上面内容后，由带教同学/用户在此确认：

- [ ] 需求理解正确，边界清晰
- [ ] 技术方案合理，没有明显更优解
- [ ] 影响面已充分考虑，没有遗漏的调用方
- [ ] 测试策略足够，验收标准清晰

**确认人**：（签名/填写时间）

**修改意见**（如有）：
